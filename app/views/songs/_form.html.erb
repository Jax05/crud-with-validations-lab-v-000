<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.radio_button(:released, true) %>
    <%= f.label(:released_true, "True") %>
    <%= f.radio_button(:released, false) %>
    <%= f.label(:released_false, "False") %>
  </div>
  <div>
    <%= f.label :release_year %><br>
    <%= f.text_field :release_year %>
  </div>
  <div>
    <%= f.label :artist_name %><br>
    <%= f.text_field :artist_name %>
  </div>
  <div>
    <%= f.label :genre %><br>
    <%= f.text_area :genre %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
