<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= radio_button_tag(:released, true) %>
    <%= label_tag(:released_true, "True") %>
    <%= radio_button_tag(:released, false) %>
    <%= label_tag(:released_false, "False") %>
  </div>
  <div>
    <%= f.label :released_year %><br>
    <%= f.text_field :released_year %>
  </div>
  <div>
    <%= f.label :artist_name %><br>
    <%= f.text_field :artist_name %>
  </div>
  <div>
    <%= f.label :genre %><br>
    <%= f.text_area :genre %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>
