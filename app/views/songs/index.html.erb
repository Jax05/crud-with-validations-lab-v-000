<h1>Here's a list of songs in our database:</h1>
<table>
  <% @songs.each do |song| %>
    <tr>
      <%= link_to song.title, song_path(song) %>
    </tr>
  <% end %>
</table>
<%= link_to 'New Song', new_song_path %>
